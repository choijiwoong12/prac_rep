# 연속된 0,1 로 그룹화 -> 더 작은 그룹 수 만큼 뒤집기
arr = list(map(int, input()))
count = 0

for i in range(1,len(arr)): # 변화하는 부분 카운트 -> +1 그룹수
  if arr[i] != arr[i-1]:
    count += 1
      
result = count//2      
      
print(result)
